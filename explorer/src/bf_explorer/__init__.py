"""bf-explorer: the terminal-explorer kit TUI extended with a BF tab.

The kit package (tt_explorer) comes from the firmware/kit submodule,
so one submodule pin versions the firmware and the UI together.
"""
